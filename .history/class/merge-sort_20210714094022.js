class MergeSort {
    constructor() {}

    sort(begin, end) {
        const size = distance(begin, end);
        if (size < 2) return;
        
        console.log("Tri fusion: ", "", " comparaisons - ");
    }
}

module.exports = MergeSort;